
<!DOCTYPE html>
<html>
<head>
  <title>Admin - Candidates</title>
  <link rel="stylesheet" href="/public/styles.css">
</head>
<body>
  <header>
    <h1>Admin - Candidates</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/logout">Logout</a>
    </nav>
  </header>
  <main>
    <% if (success && success.length) { %>
      <div class="flash success"><%= success[0] %></div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="flash error"><%= error[0] %></div>
    <% } %>

    <section>
      <h2>All Candidates</h2>
      <% if (!candidates || !candidates.length) { %>
        <p>No candidates yet.</p>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>Name</th>
              <th>Email</th>
              <th>Status</th>
              <th>Level</th>
              <th>Risk</th>
              <th>Created</th>
              <th>View</th>
            </tr>
          </thead>
          <tbody>
            <% candidates.forEach(function(c) { %>
              <tr>
                <td><%= c.full_name || '(no name)' %></td>
                <td><%= c.email %></td>
                <td><%= c.verification_status %></td>
                <td><%= c.verification_level %></td>
                <td><%= c.risk_score %></td>
                <td><%= c.created_at %></td>
                <td><a href="/admin/candidates/<%= c.id %>">Open</a></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </section>
  </main>
</body>
</html>
