
<!DOCTYPE html>
<html>
<head>
  <title>Login</title>
  <link rel="stylesheet" href="/public/styles.css">
</head>
<body>
  <header>
    <h1>Login</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/register">Candidate Register</a>
      <a href="/employer/register">Employer Register</a>
    </nav>
  </header>
  <main>
    <% if (success && success.length) { %>
      <div class="flash success"><%= success[0] %></div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="flash error"><%= error[0] %></div>
    <% } %>

    <form method="POST" action="/login" class="form">
      <label>Email
        <input type="email" name="email" required>
      </label>
      <label>Password
        <input type="password" name="password" required>
      </label>
      <button type="submit">Login</button>
    </form>
    <p>Admin login for testing: <strong>admin@example.com / admin123</strong></p>
  </main>
</body>
</html>
