
<!DOCTYPE html>
<html>
<head>
  <title>Employer Register</title>
  <link rel="stylesheet" href="/public/styles.css">
</head>
<body>
  <header>
    <h1>Employer Registration</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/login">Login</a>
      <a href="/register">Candidate Register</a>
    </nav>
  </header>
  <main>
    <% if (success && success.length) { %>
      <div class="flash success"><%= success[0] %></div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="flash error"><%= error[0] %></div>
    <% } %>

    <form method="POST" action="/employer/register" class="form">
      <label>Work Email
        <input type="email" name="email" required>
      </label>
      <label>Company Name (optional for now)
        <input type="text" name="company_name">
      </label>
      <label>Password
        <input type="password" name="password" required>
      </label>
      <label>Confirm Password
        <input type="password" name="confirmPassword" required>
      </label>
      <button type="submit">Register as Employer</button>
    </form>
  </main>
</body>
</html>
